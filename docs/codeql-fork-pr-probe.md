# Temporary CodeQL Fork PR probe

This file is only for a non-merged Draft PR on the contributor Fork. It verifies whether the Fork's default CodeQL setup runs on a same-repository pull request. It contains no application changes or test data and must not be merged.
